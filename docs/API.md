# API Notes

The API scaffold currently exposes:

- `GET /health`
- `GET /sessions/{session_id}`
- `GET /sessions/{session_id}/participants`
- `GET /sessions/{session_id}/candidate`

Additional write endpoints from the PRD can be added as session ingestion and simulator plumbing are implemented.
